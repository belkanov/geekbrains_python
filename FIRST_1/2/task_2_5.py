"""
Создать вручную список, содержащий цены на товары (10–20 товаров), например:
[57.8, 46.51, 97, ...]

A. Вывести на экран эти цены через запятую в одну строку, цена должна отображаться в виде <r> руб <kk> коп (например «5 руб 04 коп»).
   Подумать, как из цены получить рубли и копейки, как добавить нули, если, например, получилось 7 копеек или 0 копеек (должно быть 07 коп или 00 коп).

B. Вывести цены, отсортированные по возрастанию, новый список не создавать (доказать, что объект списка после сортировки остался тот же).

C. Создать новый список, содержащий те же цены, но отсортированные по убыванию.

D. Вывести цены пяти самых дорогих товаров.
   Сможете ли вывести цены этих товаров по возрастанию, написав минимум кода?

"""

# import random
#
# random.seed()
# data = [round(random.uniform(1, 10000), 2) for _ in range(10)] + [random.randint(1, 10000)]
data = [291.12, 9323.98, 5296.0, 5680.08, 4867.5, 5376.96, 73.24, 7526.01, 8552.87, 3228.17, 9771]

id_1 = id(data)
print(f'id = {id_1}\n{data}')

print('\n----- A -----')
# добавил немного удобства в виде разделителя десятков. Хотя это по хорошему надо обсуждать с заказчиком =)
print(', '.join([f'{int(x):,} руб {int(x % 1 * 100):02} коп' for x in data]))

print('\n----- B -----')
data.sort()
id_2 = id(data)
print(data)
print(f'Список тот же? {id_1 == id_2}')

print('\n----- C -----')
data_sort_reverse = sorted(data, reverse=True)
id_3 = id(data_sort_reverse)
print(data_sort_reverse)
print(f'Это новый список? {id_1 != id_3}')

print('\n----- D -----')
print(data[len(data) - 5:])
